export type PoolChildOption = {
  id: string;
  firstName: string;
  lastName: string;
  schoolId: string | null;
};

export type PoolAssistantOption = {
  id: string;
  name: string;
  email: string;
};
